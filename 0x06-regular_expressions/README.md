# Regular expresitions

Grind those expressions that are used regullary.

The following ruby code template is used.
Only the `regex` part has any useful changes

```ruby
#!/usr/bin/env ruby
puts ARGV[0].scan(/regex/).join
```
